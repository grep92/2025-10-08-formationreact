import React from "react";
import "./Button.css";

const Button = (props) => {
  return (
    <button className="Button" data-testid="Button">
      Button Component
    </button>
  );
};

export default Button;
